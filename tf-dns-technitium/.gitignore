.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
tfplan
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Real internal topology (domain, IP assignments) - not committed HERE; it's a
# symlink into homelab-pipeline-config (private), same treatment now also
# used for tf-pve-netbox-ipam's hydration.tf. The symlink itself is
# machine-local wiring, not portable, so it stays gitignored too - recreate
# it after a fresh clone:
#   ln -s ../../homelab-pipeline-config/tf-dns-technitium/records.tf records.tf
records.tf
