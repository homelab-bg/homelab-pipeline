.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
tfplan
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Real internal topology (real hostnames/IPs) - not committed HERE; it's a
# symlink into homelab-pipeline-config (private), same as tf-dns-technitium's
# records.tf. The symlink itself is machine-local wiring, not portable, so it
# stays gitignored too - recreate it after a fresh clone:
#   ln -s ../../homelab-pipeline-config/tf-pve-netbox-ipam/hydration.tf hydration.tf
hydration.tf
